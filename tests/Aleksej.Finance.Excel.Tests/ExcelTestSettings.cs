// The add-in to load is specified per test class via [ExcelTestSettings(AddIn = "...")]
// ([ExcelTestSettings] is only valid on classes, not at assembly level). See the test
// classes under Tests\. The add-in .xll is copied next to this assembly by the
// CopyAddInXll build target.
